[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["corepack enable && corepack prepare pnpm@8.15.0 --activate"]

[phases.install]
dependsOn = ["setup"]
cmds = ["cd .. && pnpm install --frozen-lockfile"]

[start]
cmd = "cd .. && pnpm db:migrate && pnpm --filter server start"
